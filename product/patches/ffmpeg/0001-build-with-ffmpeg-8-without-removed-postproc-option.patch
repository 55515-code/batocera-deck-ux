--- a/configure
+++ b/configure
@@ -4666,6 +4666,10 @@
         --enable-sdl)
             enable sdl2
         ;;
+        --enable-postproc|--disable-postproc)
+            # FFmpeg 8 removed the standalone postproc library.
+            # Keep the legacy Buildroot option accepted as a no-op.
+        ;;
         --enable-lto*)
             lto=-f${opt#--enable-}
         ;;
